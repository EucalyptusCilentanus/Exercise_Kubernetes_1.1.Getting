apiVersion: v1
kind: Service
metadata:
  name: the-project-svc
spec:
  type: NodePort
  selector:
    app: the-project
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30080
